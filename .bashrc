alias ngc='nest g co'       # Alias for generating a controller
alias ngm='nest g module'   # Alias for generating a module
alias ngs='nest g service'  # Alias for generating a service
alias ngi='nest g interceptor'  # Alias for generating an interceptor
alias ngp='nest g pipe'  # Alias for generating a pipe

alias pa="php artisan"
alias pam="php artisan migrate"
alias pamf="php artisan migrate:fresh"
alias pamfs="php artisan migrate:fresh --seed"
alias pat="php artisan test"
alias pas="php artisan serve"